apiVersion: apps/v1
kind: Deployment
metadata:
  name: sub-app-deployment
  labels: 
    app: sub-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sub-app
  template:
    metadata:
      labels:
        app: sub-app
    spec:
      containers:
      - name: sub-app
        image: yzhbankov/sub-app
        ports:
        - containerPort: 3000
          name: node-port
        - containerPort: 7001
          name: zmq-port
---

apiVersion: v1
kind: Service
metadata:
  name: sub-service
spec:
  selector:
    app: sub-app
  type: LoadBalancer
  ports:
  - protocol: TCP
    name: http-3000
    port: 3000
    targetPort: 3000
    nodePort: 30001
  - protocol: TCP
    name: tcp-7001
    port: 7001
    targetPort: 7001
    