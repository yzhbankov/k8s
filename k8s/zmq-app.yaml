apiVersion: apps/v1
kind: Deployment
metadata:
  name: zmq-app-deployment
  labels: 
    app: zmq-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zmq-app
  template:
    metadata:
      labels:
        app: zmq-app
    spec:
      containers:
      - name: zmq-app
        image: yzhbankov/zmq-app
        ports:
        - containerPort: 3001
          name: server-port
        - containerPort: 7001
          name: zmq-port
---
apiVersion: v1
kind: Service
metadata:
  name: zmq-app-service
spec:
  selector:
    app: zmq-app
  type: LoadBalancer
  ports:
  - protocol: TCP
    name: http-3001
    port: 3001
    targetPort: 3001
    nodePort: 30002
  - protocol: TCP
    name: tcp-7001
    port: 7001
    targetPort: 7001

    